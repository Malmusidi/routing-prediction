# Notebooks

**`03_apply_pretrained_models.ipynb`**

This notebook loads the pre-trained models and generates predictions for O-D pairs. The only need is to provide origin and destination coordinates.

## All Notebooks

| Notebook | Purpose | When to Use |
|----------|---------|-------------|
| `01_generate_predictors.ipynb` | Generate the 6 predictor features | Only if you want to understand how features are calculated |
| `02_train_models.ipynb` | Train new models | Only if you want to train models on your own data |
| `03_apply_pretrained_models.ipynb` | Apply pre-trained models | **Start here** |
